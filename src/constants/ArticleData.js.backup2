export const API_BASE = import.meta.env.VITE_API_URL || "http://localhost:5000";

export const getHeroImages = (slug) => [];
export const getGalleryImages = (slug) => [];
export const getMainVideo = (slug) => null; // ✅ ویدیوی پیش‌فرض حذف شد
export const getRelatedVideos = (slug) => []; // ✅ ویدیوی مرتبط پیش‌فرض حذف شد

export default {};